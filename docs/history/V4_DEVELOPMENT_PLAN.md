# LexiScene V4 开发计划

## 1. 版本目标

本版本提升词汇输入能力、统一英文阅读题体验，并建立两条不同的内容质量通道：

- 用户私人生成文章强调快速、即时，不进入管理员审核。
- 每日公开文章强调稳定、可核查，继续经过程序校验、AI 审核和人工发布。

现有 React、Cloudflare Worker、D1、IndexedDB 和账号同步架构保持不变。

## 2. 开发范围

### 2.1 PDF 和图片导入单词

- 支持文本型 PDF、扫描版 PDF、JPG、PNG 和 WebP。
- 文本型 PDF 使用 `pdfjs-dist` 在浏览器提取文字。
- 图片和扫描版 PDF 使用 `tesseract.js` 在浏览器执行英文 OCR。
- OCR 和 PDF 模块按需加载，不进入首页首屏包。
- 原始文件不上传 Worker，也不保存到 D1。
- 从识别结果提取英文单词和词组，过滤页码、标点、标题和乱码。
- 复用现有词形归一、去重和词典查询逻辑。
- 保存前展示候选词预览，支持勾选、编辑和删除。
- 中文释义由内置词典补充，不保留图片中的原始中文释义。
- 文件限制：PDF 最大 20 MB、最多 50 页；图片单张最大 10 MB。

### 2.2 固定英文阅读题

- 用户生成文章和每日文章的题干固定使用英文。
- 每题固定四个英文选项。
- 页面操作提示继续使用中文。
- 现有文章正文不重写，只更新不符合要求的题目。

### 2.3 题目翻译

题目 JSON 扩展为：

```ts
interface Question {
  question: string;
  options: [string, string, string, string];
  answer: 0 | 1 | 2 | 3;
  questionZh?: string;
  optionsZh?: [string, string, string, string];
  evidence?: string;
}
```

- 英文题目始终显示。
- 中文翻译在生成时一并产生，不在点击时调用翻译 API。
- 每题提供翻译显示/隐藏按钮。
- 阅读页提供全部翻译/全部隐藏操作。
- 中文翻译显示在对应英文内容下方，不替换英文。
- `evidence` 用于质量检查，默认不展示给普通用户。
- `questions_json` 已能保存扩展字段，不增加数据库列。

### 2.4 用户私人文章快速质量通道

流程：

1. 清洗、去重并归一用户单词。
2. 一次模型请求生成文章、英文题目、中文翻译、答案和证据句。
3. Worker 执行无额外网络请求的快速校验。
4. 校验通过后立即返回并保存。
5. 硬性校验失败时最多调用一次局部修复。
6. 第二次仍失败则返回失败，本次不计入每日成功额度。

快速校验项目：

- JSON 和字段结构。
- 文章字数、段落数量和平均句长。
- 目标单词命中率和过度重复。
- 题干及选项英文比例。
- 每题四个选项和合法答案索引。
- 题目、选项重复。
- 证据句是否能在正文中找到。
- 明显乱码、模型说明文字和不安全的具体事实断言。

局部修复规则：

- 题目问题只重做题目。
- 翻译缺失只补充翻译。
- 字数不足只扩写正文。
- 不执行完整 AI 审核，不进入管理员后台。

### 2.5 每日公开文章严格质量通道

- 素材优先使用政府机构、国际组织、大学和公共研究机构等可核查来源。
- 标题和 RSS 摘要只能作为选题线索，不能支撑未经来源支持的具体事实。
- 生成后先执行与私人文章相同的程序校验。
- DeepSeek 审核输出总分、分项评分、问题、事实核查建议和题目证据检查。
- 不合格文章最多自动返修两次。
- 返修后重新执行程序校验和 AI 审核。
- 管理员页面展示审核结果、来源、证据和返修信息。
- 只有管理员确认后才能发布到文章池。

## 3. 数据与接口原则

- PDF 和图片文件只在浏览器内处理。
- D1 只保存最终词汇、文章和结构化题目。
- 新的 Question 字段必须兼容没有翻译的历史记录。
- 本地文章和云端文章继续使用稳定 ID、`createdAt` 和 `updatedAt`。
- 用户私人文章继续按账号隔离和离线同步。
- 生成失败和修复失败不计入每日十篇成功额度。

## 4. 实施顺序

1. 扩展 Question 类型、Worker Schema 和兼容解析。
2. 修改两条生成链路，固定英文题目并生成翻译和证据。
3. 实现阅读页单题翻译和全部翻译交互。
4. 实现私人文章快速校验和一次局部修复。
5. 实现 PDF 文本提取和图片/扫描 PDF OCR。
6. 实现候选词预览、编辑、选择和保存。
7. 增强每日文章程序校验、AI 审核和自动返修。
8. 更新现有文章题目处理脚本。
9. 执行类型检查、生产构建、Worker 检查和浏览器测试。
10. 更新开发日志并推送 GitHub。

## 5. 验收标准

- 文本 PDF 可以提取英文候选词。
- 扫描 PDF 和清晰图片可以完成英文 OCR。
- OCR 失败、超限和不支持格式有明确提示。
- 用户可以在保存前编辑和取消候选词。
- 所有新生成题目和四个选项均为英文。
- 单题和全部题目翻译可以独立显示与隐藏。
- 翻译操作不产生新的 API 请求。
- 私人文章通常只执行一次模型调用。
- 硬性失败最多局部修复一次，失败不扣成功额度。
- 每日文章保留 AI 审核和管理员确认发布。
- 历史题目没有翻译字段时仍可正常阅读和答题。
- IndexedDB、D1 同步和账号隔离不发生回归。

## 6. 本版本不做

- 不把 PDF 或图片上传到云端长期保存。
- 不承诺识别手写体或复杂教材排版。
- 不保留图片中的原始中文释义。
- 不为私人生成文章增加管理员审核。
- 不在点击翻译时调用第三方翻译 API。
- 不修改每日文章两日轮换规则。
